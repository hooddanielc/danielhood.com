import Ember from 'ember';

/**
 * An object that represents geometry.
 *
 * @class Geometry
 */
export default Ember.Object.extend({
  intialize: function () {
    console.log('TODO');
  }.on('init')
});
